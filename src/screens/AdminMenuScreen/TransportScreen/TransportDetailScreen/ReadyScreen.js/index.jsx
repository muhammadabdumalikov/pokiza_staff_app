import React from "react";
import { View, ScrollView, Text } from "react-native";

const ReadyScreen = () => {
    return(
        <ScrollView>

        </ScrollView>
    )
}

export default ReadyScreen;