import React from "react";
import { View, ScrollView, Text } from "react-native";

const WaitingScreen = () => {
    return(
        <ScrollView>

        </ScrollView>
    )
}

export default WaitingScreen;